name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    name: Create Release
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '14'

      - name: Install dependencies
        run: npm install

      - name: Build for production
        run: npm run build:prod

      - name: Create release archive
        run: |
          cd dist
          zip -r ../bcp-admin-ui-${{ github.ref_name }}.zip .
          cd ..
          tar -czvf bcp-admin-ui-${{ github.ref_name }}.tar.gz -C dist .

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            bcp-admin-ui-${{ github.ref_name }}.zip
            bcp-admin-ui-${{ github.ref_name }}.tar.gz
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
